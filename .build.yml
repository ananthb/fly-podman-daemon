image: archlinux
secrets:
- 2630423b-b29c-44d3-813f-5284b32fb8b0
- 455093dd-02f6-485a-b5bb-c7b3491c5725
packages:
  - curl
  - docker
tasks:
  - setup: |
      curl -L https://fly.io/install.sh | sudo env FLYCTL_INSTALL=/usr/local sh
      sudo systemctl start docker.service
  - deploy: |
      flyctl deploy --detach --local-only podman-daemon
